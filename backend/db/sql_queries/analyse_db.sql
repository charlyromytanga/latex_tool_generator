-- SELECT name FROM sqlite_master WHERE type='table' AND name NOT LIKE 'sqlite_%';

-- Infos sur la table cv_base_in_all
-- PRAGMA table_info(cv_base_in_all);
-- Clés étrangères
--PRAGMA foreign_key_list(cv_base_in_all);

-- Infos sur la table job_offer
--PRAGMA table_info(job_offer);
-- Clés étrangères
-- PRAGMA foreign_key_list(job_offer);

-- Infos sur la table candidature_tracking
-- PRAGMA table_info(candidature_tracking);
-- Clés étrangères
-- PRAGMA foreign_key_list(candidature_tracking);



