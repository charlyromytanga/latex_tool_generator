# services/matching

FR : Service de matching. Implémente l'algorithme de similarité, pondération, scoring et expose l'API `/match`.

EN: Matching service. Implements similarity, weighting, scoring algorithms and exposes the `/match` API.
