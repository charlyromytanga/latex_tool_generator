# streamlit

FR : Ce dossier contient l’interface utilisateur développée avec Streamlit. Il consomme l’API backend (FastAPI) pour proposer une expérience interactive (upload d’offres, scoring, génération de CV, etc.).

EN: This folder contains the user interface built with Streamlit. It consumes the backend API (FastAPI) to provide an interactive experience (offer upload, scoring, CV generation, etc.).
